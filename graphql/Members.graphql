query Members($companyId: ID!) {
  members(companyId: $companyId) {
    __typename
    ... on CompanyMember {
      role
      user {
        id
        firstName
        lastName
        email
        avatarUrl
      }
    }
    ... on CompanyInvite {
      role
      recipientEmail
    }
  }
}
