query Job($id: ID!) {
  job(id: $id) {
    id
    title
    deadline
    description
    questions {
      id
      question
      time
    }
    company {
      id
      name
      avatarUrl
    }
  }
}
